Property of Carson Krueger

The Program is implemented to recieve an inputed string as an argument into the 
longestPalindrome() function. Then the function will return the longest 
palindrome string inside of the inputed string. 
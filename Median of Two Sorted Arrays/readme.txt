Property of Carson Krueger

The Program is implemented to find the median from two seperate lists, comprised of integers, and
returning a floating point number by using the findMedianSortedArrays() function within the Solution
class. The lists can be modified within the main() function named list1 and list2.
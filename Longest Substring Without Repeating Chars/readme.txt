Property of Carson Krueger

The Program is implemented to find the longest non-repeating substring within the variable
's' inside of the main() function. After the Program has finished computing the 
lengthOfLongestSubstring() function will return the longest substring integer.